import { startLogin } from "@/const";
import React, { useEffect } from "react";
import "../hero-product-loop.css";
import { AgentFenceLogo } from "@/components/AgentFenceLogo";
import { ArrowRight, Bot, CheckCircle2, ChevronRight, Eye, KeyRound, Landmark, LockKeyhole, Network, ScanEye, ShieldCheck, Sparkles, Stethoscope, Workflow, Zap } from "lucide-react";
import { Link } from "wouter";

type AuthMode = "signin" | "signup";

export function AuthPanel({ mode, onAuthenticate = startLogin }: { mode: AuthMode; onAuthenticate?: () => void }) {
  const isSignUp = mode === "signup";
  return <main className="auth-experience"><div className="auth-aurora auth-aurora-one" /><div className="auth-aurora auth-aurora-two" /><section className="auth-shell"><Link href="/" className="auth-brand"><AgentFenceLogo /><span>Back to overview</span></Link><div className="auth-card glass-panel"><div className="auth-shield"><ShieldCheck size={22} /></div><div><p className="eyebrow">{isSignUp ? "Build your control plane" : "Protected access"}</p><h1>{isSignUp ? "Give every agent a security boundary." : "Welcome back to AgentFence."}</h1><p className="auth-copy">{isSignUp ? "Create your security workspace. Your first sign-in provisions an isolated organization for agents, policies, and evidence." : "Sign in to manage the identity, permissions, data, and decisions behind your AI agents."}</p></div><button className="auth-google" onClick={onAuthenticate}><span className="google-mark">G</span><span>Continue with Google</span><ArrowRight size={17} /></button><button className="auth-sso" onClick={onAuthenticate}><KeyRound size={16} /><span>Continue with secure SSO</span></button><div className="auth-divider"><span /> <small>Secure identity portal</small> <span /></div><p className="auth-note"><LockKeyhole size={14} /> Google and SSO availability is determined by your organization’s secure identity portal. AgentFence never collects your password.</p><p className="auth-switch">{isSignUp ? "Already have a workspace?" : "New to AgentFence?"} <Link href={isSignUp ? "/signin" : "/signup"}>{isSignUp ? "Sign in" : "Create your workspace"}<ChevronRight size={14} /></Link></p></div><p className="auth-footer">Zero-trust control plane for agent actions.</p></section></main>;
}

export function SignInPage() { return <AuthPanel mode="signin" />; }
export function SignUpPage() { return <AuthPanel mode="signup" />; }

const useCases = [
  { icon: Landmark, tag: "Finance operations", title: "Allow invoice reading. Gate payments.", text: "Let an assistant reconcile invoices while AgentFence requires human approval before it can create a payment or change bank details.", tone: "amber" },
  { icon: Stethoscope, tag: "Healthcare workflows", title: "Keep PHI in approved lanes.", text: "Detect sensitive patient data, prevent unapproved model or destination access, and retain evidence for review.", tone: "violet" },
  { icon: Bot, tag: "Customer support", title: "Make helpful agents accountable.", text: "Allow agents to search approved records while blocking exports, privilege changes, and unsafe browser actions.", tone: "cyan" },
];

const pricingPlans = [
  { name: "Pilot", price: "$99", detail: "per workspace / month", description: "A focused first governed production workflow.", features: ["Up to 3 governed agents", "Policies, approvals, Action Capture, and evidence export", "One pilot workspace"] },
  { name: "Growth", price: "$299", detail: "per workspace / month", description: "For cross-functional agent deployment and expanded oversight.", features: ["Up to 20 governed agents", "Teams, advanced observability, and connection profiles", "Priority pilot support"], featured: true },
  { name: "Enterprise", price: "Custom", detail: "annual agreement", description: "For regulated, multi-team, and custom-control deployments.", features: ["Custom capacity and rollout design", "Vault, SIEM/SOAR, and IdP/SCIM activation assistance", "Architecture and procurement support"] },
];

export function LandingPage() {
  const architectureImage = "/manus-storage/agentfence_claim_hardened_architecture_946b4c2e.png";
  useEffect(() => {
    const visual = document.querySelector<HTMLElement>(".containment-visual");
    if (!visual) return;
    const loop = document.createElement("div");
    loop.className = "hero-product-loop";
    loop.setAttribute("aria-hidden", "true");
    const video = document.createElement("video");
    video.muted = true;
    video.loop = true;
    video.autoplay = true;
    video.playsInline = true;
    video.preload = "metadata";
    video.src = "/manus-storage/agentfence-hero-product-loop_3c9eda4e.mp4";
    loop.appendChild(video);
    visual.prepend(loop);
    const preference = window.matchMedia("(prefers-reduced-motion: reduce)");
    const syncPlayback = () => {
      if (preference.matches) video.pause();
      else void video.play().catch(() => undefined);
    };
    syncPlayback();
    preference.addEventListener("change", syncPlayback);
    return () => {
      preference.removeEventListener("change", syncPlayback);
      loop.remove();
    };
  }, []);
  return <main className="marketing-shell containment-landing"><div className="marketing-orb orb-left" /><div className="marketing-orb orb-right" /><header className="marketing-nav"><Link href="/" className="brand-link"><AgentFenceLogo /></Link><nav><a href="#how-it-works">Control flow</a><a href="#architecture">Architecture</a><a href="#use-cases">Use cases</a><a href="#pricing">Pricing</a><Link href="/signin">Sign in</Link><Link href="/signup" className="nav-cta">Build your boundary <ArrowRight size={15} /></Link></nav></header><section className="marketing-hero"><div className="hero-copy"><div className="hero-eyebrow containment-eyebrow"><span className="pulse-dot" /> AI agent containment layer</div><h1>The red line between<br /><em>agents and consequences.</em></h1><p>For cloud agents and managed browser wrappers that use the integrated control path, AgentFence evaluates identity, policy, sensitive data, destination, and human approval before a consequential tool action is released.</p><div className="hero-actions"><Link href="/signup" className="hero-primary">Build your control plane <ArrowRight size={17} /></Link><a href="#how-it-works" className="hero-secondary"><Eye size={17} /> Trace the decision path</a></div><div className="hero-trust"><span><CheckCircle2 size={15} /> Scope verified</span><span><CheckCircle2 size={15} /> Data guarded</span><span><CheckCircle2 size={15} /> Evidence chained</span></div></div><div className="hero-visual glass-panel containment-visual"><div className="visual-topline"><span>Containment decision path</span><span className="visual-live"><i /> Boundary active</span></div><div className="containment-line"><span>Agent intent</span><b /><span>AgentFence</span><b /><span>Tool execution</span></div><div className="agent-orbit"><div className="orbit-ring ring-a" /><div className="orbit-ring ring-b" /><div className="orbit-node agent-node"><Bot size={22} /><span>AI agent</span></div><div className="orbit-node fence-node"><ShieldCheck size={30} /><span>Containment</span></div><div className="orbit-node tool-node"><Network size={20} /><span>Tools & APIs</span></div></div><div className="decision-card containment-decision"><div className="decision-status"><span className="decision-icon"><LockKeyhole size={15} /></span><div><small>High-impact action intercepted</small><strong>Export customer records</strong></div><span className="approval-pill">Human gate</span></div><div className="policy-line"><span>Policy match</span><code>export.customer_data</code><b>→</b><span>Approval required</span></div></div></div></section><section className="wall-banner"><div><span className="eyebrow">The simple model</span><h2>Network firewall logic for AI actions.</h2></div><p>Traditional firewalls ask whether a machine may reach an IP and port. On its integrated action path, AgentFence asks whether a verified AI identity may use a specific tool, for a specific action, with specific data, against a specific destination.</p></section><section id="how-it-works" className="flow-section"><div className="section-heading"><p className="eyebrow">How AgentFence works</p><h2>Every integrated action crosses the boundary.</h2><p>Think of the Tool Gateway as a policy-enforcing checkpoint for integrated agentic systems—not a passive activity log.</p></div><div className="flow-track"><div className="flow-step"><span className="step-number">01</span><Bot size={22} /><h3>Agent requests an action</h3><p>An integrated agent asks to call an API, open a browser, query data, or change a record.</p></div><div className="flow-connector"><i /><ArrowRight size={17} /></div><div className="flow-step active"><span className="step-number">02</span><ShieldCheck size={24} /><h3>AgentFence evaluates it</h3><p>Identity, policy, scope, inbound and outbound data sensitivity, target destination, and risk are evaluated together.</p></div><div className="flow-connector"><i /><ArrowRight size={17} /></div><div className="flow-step"><span className="step-number">03</span><Zap size={22} /><h3>Allow, block, or escalate</h3><p>Allowed integrated actions proceed; risky actions are blocked or routed to an authorized reviewer.</p></div></div></section><section id="architecture" className="architecture-showcase"><div className="section-heading"><p className="eyebrow">Enterprise architecture</p><h2>One integrated control path across cloud, browser, and enterprise systems.</h2><p>Cloud SDK and managed-browser wrapper actions converge on the same decision point. Browser execution uses the existing enterprise session; Vault-backed target credential operations are optional and customer-activated.</p></div><div className="architecture-grid"><figure className="architecture-image-frame glass-panel"><img src={architectureImage} alt="AgentFence integrated-action architecture showing Data Guard as policy input, cloud and managed browser paths, optional Vault AppRole activation, browser-session execution boundaries, and SOC evidence flows." loading="lazy" /><figcaption><Workflow size={16} /> Integrated action model: identity → gateway → Data Guard and policy → approval or scoped target path → privacy-safe evidence.</figcaption></figure><div className="architecture-story"><article><span>01</span><div><h3>Connect a workload</h3><p>Register a cloud SDK integration or managed browser wrapper with a tenant-bound identity and a clear owner.</p></div></article><article><span>02</span><div><h3>Control each integrated action</h3><p>The Tool Gateway evaluates policy, strongest detected data sensitivity, destination, short-lived runtime scope, and human approval requirements before release.</p></div></article><article><span>03</span><div><h3>Prove what happened</h3><p>Action Capture, Action Trace, audit evidence, notifications, and configured SOC workflows retain a privacy-safe decision record.</p></div></article><Link href="/signup" className="hero-primary">Start with one governed agent <ArrowRight size={17} /></Link></div></div></section><section id="use-cases" className="usecase-section"><div className="section-heading"><p className="eyebrow">Start with the risk that matters</p><h2>One containment layer. Every integrated agent workflow.</h2></div><div className="usecase-grid">{useCases.map(({ icon: Icon, tag, title, text, tone }) => <article key={tag} className={`usecase-card glass-panel usecase-${tone}`}><div className="usecase-icon"><Icon size={21} /></div><p>{tag}</p><h3>{title}</h3><span>{text}</span><Link href="/signup">Explore the control model <ArrowRight size={15} /></Link></article>)}</div></section><section id="pricing" className="pricing-section"><div className="section-heading"><p className="eyebrow">Feature-based plans</p><h2>Start narrow. Expand control with evidence.</h2><p>Pricing reflects platform access and pilot-support scope. It does not promise a particular ROI, savings amount, or security outcome.</p></div><div className="public-pricing-grid">{pricingPlans.map(plan => <article key={plan.name} className={`public-pricing-card glass-panel ${plan.featured ? "featured" : ""}`}><div><p>{plan.featured ? "Recommended" : "AgentFence plan"}</p><h3>{plan.name}</h3><span>{plan.description}</span></div><div className="public-pricing-price"><strong>{plan.price}</strong><small>{plan.detail}</small></div><ul>{plan.features.map(feature => <li key={feature}><CheckCircle2 size={15} /> {feature}</li>)}</ul><Link href="/signup" className={plan.featured ? "hero-primary" : "hero-secondary"}>{plan.name === "Enterprise" ? "Request design review" : `Choose ${plan.name}`} <ArrowRight size={15} /></Link></article>)}</div></section><section className="marketing-cta glass-panel"><div><p className="eyebrow">Security should enable adoption</p><h2>Let agents move quickly. Keep authority contained.</h2><p>Start with a workspace, register one agent, write one boundary policy, and inspect every integrated decision in context.</p></div><Link href="/signup" className="hero-primary">Build your boundary <Sparkles size={17} /></Link></section><footer className="marketing-footer"><AgentFenceLogo /><span>AgentFence · Containment-first governance for AI agents</span><Link href="/signin">Sign in</Link></footer></main>;
}
