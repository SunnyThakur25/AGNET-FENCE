import { startLogin } from "@/const";
import React from "react";
import { AgentFenceLogo } from "@/components/AgentFenceLogo";
import { ArrowRight, Bot, CheckCircle2, ChevronRight, Eye, KeyRound, Landmark, LockKeyhole, Network, ScanEye, ShieldCheck, Sparkles, Stethoscope, Zap } from "lucide-react";
import { Link } from "wouter";

type AuthMode = "signin" | "signup";

export function AuthPanel({ mode, onAuthenticate = startLogin }: { mode: AuthMode; onAuthenticate?: () => void }) {
  const isSignUp = mode === "signup";
  return <main className="auth-experience"><div className="auth-aurora auth-aurora-one" /><div className="auth-aurora auth-aurora-two" /><section className="auth-shell"><Link href="/" className="auth-brand"><AgentFenceLogo /><span>Back to overview</span></Link><div className="auth-card glass-panel"><div className="auth-shield"><ShieldCheck size={22} /></div><div><p className="eyebrow">{isSignUp ? "Build your control plane" : "Protected access"}</p><h1>{isSignUp ? "Give every agent a security boundary." : "Welcome back to AgentFence."}</h1><p className="auth-copy">{isSignUp ? "Create your security workspace. Your first sign-in provisions an isolated organization for agents, policies, and evidence." : "Sign in to manage the identity, permissions, data, and decisions behind your AI agents."}</p></div><button className="auth-google" onClick={onAuthenticate}><span className="google-mark">G</span><span>Continue with Google</span><ArrowRight size={17} /></button><button className="auth-sso" onClick={onAuthenticate}><KeyRound size={16} /><span>Continue with secure SSO</span></button><div className="auth-divider"><span /> <small>Secure identity portal</small> <span /></div><p className="auth-note"><LockKeyhole size={14} /> Google and SSO availability is determined by your organization’s secure identity portal. AgentFence never collects your password.</p><p className="auth-switch">{isSignUp ? "Already have a workspace?" : "New to AgentFence?"} <Link href={isSignUp ? "/signin" : "/signup"}>{isSignUp ? "Sign in" : "Create your workspace"}<ChevronRight size={14} /></Link></p></div><p className="auth-footer">Zero-trust control plane for agent actions.</p></section></main>;
}

export function SignInPage() { return <AuthPanel mode="signin" />; }
export function SignUpPage() { return <AuthPanel mode="signup" />; }

const useCases = [
  { icon: Landmark, tag: "Finance operations", title: "Allow invoice reading. Gate payments.", text: "Let an assistant reconcile invoices while AgentFence requires human approval before it can create a payment or change bank details.", tone: "amber" },
  { icon: Stethoscope, tag: "Healthcare workflows", title: "Keep PHI in approved lanes.", text: "Detect sensitive patient data, prevent unapproved model or destination access, and retain evidence for review.", tone: "violet" },
  { icon: Bot, tag: "Customer support", title: "Make helpful agents accountable.", text: "Allow agents to search approved records while blocking exports, privilege changes, and unsafe browser actions.", tone: "cyan" },
];

export function LandingPage() {
  return <main className="marketing-shell"><div className="marketing-orb orb-left" /><div className="marketing-orb orb-right" /><header className="marketing-nav"><Link href="/" className="brand-link"><AgentFenceLogo /></Link><nav><a href="#how-it-works">How it works</a><a href="#use-cases">Use cases</a><Link href="/signin">Sign in</Link><Link href="/signup" className="nav-cta">Start securely <ArrowRight size={15} /></Link></nav></header><section className="marketing-hero"><div className="hero-copy"><div className="hero-eyebrow"><span className="pulse-dot" /> Agent firewall for the AI era</div><h1>AI agents move fast.<br /><em>Trust needs a gate.</em></h1><p>AgentFence sits between an AI agent and the systems it can touch. It evaluates every tool call against identity, policy, data sensitivity, destination, and human approval rules—before the action happens.</p><div className="hero-actions"><Link href="/signup" className="hero-primary">Create your security workspace <ArrowRight size={17} /></Link><a href="#how-it-works" className="hero-secondary"><Eye size={17} /> See the control flow</a></div><div className="hero-trust"><span><CheckCircle2 size={15} /> Identity-bound</span><span><CheckCircle2 size={15} /> Policy-enforced</span><span><CheckCircle2 size={15} /> Evidence-ready</span></div></div><div className="hero-visual glass-panel"><div className="visual-topline"><span>Live decision path</span><span className="visual-live"><i /> Enforced</span></div><div className="agent-orbit"><div className="orbit-ring ring-a" /><div className="orbit-ring ring-b" /><div className="orbit-node agent-node"><Bot size={22} /><span>AI agent</span></div><div className="orbit-node fence-node"><ShieldCheck size={30} /><span>AgentFence</span></div><div className="orbit-node tool-node"><Network size={20} /><span>Tools & APIs</span></div></div><div className="decision-card"><div className="decision-status"><span className="decision-icon"><LockKeyhole size={15} /></span><div><small>High-impact action</small><strong>Export customer records</strong></div><span className="approval-pill">Approval required</span></div><div className="policy-line"><span>Policy match</span><code>export.customer_data</code><b>→</b><span>Human gate</span></div></div></div></section><section className="wall-banner"><div><span className="eyebrow">The simple model</span><h2>Network firewall for AI actions.</h2></div><p>Traditional firewalls ask whether a machine may reach an IP and port. AgentFence asks whether an AI agent may use a specific tool, for a specific action, with specific data, against a specific destination.</p></section><section id="how-it-works" className="flow-section"><div className="section-heading"><p className="eyebrow">How AgentFence works</p><h2>Every action crosses a decision boundary.</h2><p>Think of the Tool Gateway as an application-layer checkpoint built for agentic systems.</p></div><div className="flow-track"><div className="flow-step"><span className="step-number">01</span><Bot size={22} /><h3>Agent requests an action</h3><p>An agent asks to call an API, open a browser, query data, or change a record.</p></div><div className="flow-connector"><i /><ArrowRight size={17} /></div><div className="flow-step active"><span className="step-number">02</span><ShieldCheck size={24} /><h3>AgentFence evaluates it</h3><p>Identity, policy, scope, data sensitivity, target destination, and risk are evaluated together.</p></div><div className="flow-connector"><i /><ArrowRight size={17} /></div><div className="flow-step"><span className="step-number">03</span><Zap size={22} /><h3>Allow, block, or escalate</h3><p>Safe actions proceed; risky actions are blocked or routed to an authorized reviewer.</p></div></div></section><section id="use-cases" className="usecase-section"><div className="section-heading"><p className="eyebrow">Start with the risk that matters</p><h2>One control plane. Many agent workflows.</h2></div><div className="usecase-grid">{useCases.map(({ icon: Icon, tag, title, text, tone }) => <article key={tag} className={`usecase-card glass-panel usecase-${tone}`}><div className="usecase-icon"><Icon size={21} /></div><p>{tag}</p><h3>{title}</h3><span>{text}</span><Link href="/signup">Explore the control model <ArrowRight size={15} /></Link></article>)}</div></section><section className="marketing-cta glass-panel"><div><p className="eyebrow">Security should enable adoption</p><h2>Build agents your team can actually trust.</h2><p>Start with a workspace, register one agent, write one policy, and see every decision in context.</p></div><Link href="/signup" className="hero-primary">Create your workspace <Sparkles size={17} /></Link></section><footer className="marketing-footer"><AgentFenceLogo /><span>AgentFence · Zero-trust governance for AI agents</span><Link href="/signin">Sign in</Link></footer></main>;
}
